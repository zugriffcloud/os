---
title: 'Mailman'
description: A guide on how to send emails with zugriff at the Edge.
---

:::danger
Outgoing traffic on ports `25` and `465` is blocked. Use port `587` with STARTTLS instead.
:::

:::note
This page is a re-export of the `README.md` file of the [`@zugriff/mailman`](https://npmjs.com/@zugriff/mailman) package.
:::

import README from '@zugriff/mailman/README.md';

<README />
